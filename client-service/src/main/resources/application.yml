spring:
  application:
    name: client-service

  datasource:
    url: jdbc:sqlserver://sqlserver:1433;databaseName=BancosClientes;encrypt=true;trustServerCertificate=true
    username: sa
    password: Glob19Ast35XX$
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect

server:
  port: 8081
